# Fly.io config for tradeworkdesk api-server (peak profile)
# Deploy peak profile from monorepo root: fly deploy -c fly.peak.toml
app = "tradeworkdesk-api"
primary_region = "lhr"

[build]
  dockerfile = "artifacts/api-server/Dockerfile"

[env]
  NODE_ENV = "production"
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  # Keep at least one machine running and allow burst scaling
  min_machines_running = 1
  # Cap autoscaling at four machines
  max_machines_running = 4

  [http_service.concurrency]
    type = "requests"
    # Scale out sooner in peak mode to prioritise response time over cost
    soft_limit = 40
    hard_limit = 60

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
