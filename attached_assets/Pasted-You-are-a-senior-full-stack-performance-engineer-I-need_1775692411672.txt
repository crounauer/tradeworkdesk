You are a senior full-stack performance engineer. I need you to audit and refactor my app for production-grade performance, specifically fixing a slow authenticated page refresh path.

Context

My app is built in Replit.

Current behaviour:

If I log out and log back in, performance is much better
If I am already logged in and refresh the page, it takes ages
Initial UI shell appears quickly
The remaining delay is in authenticated bootstrap requests, especially endpoints like:
/api/init
/api/homepage

Recent browser evidence suggests:

request count has already been reduced
the main remaining bottleneck is no longer many requests
the main issue is the logged-in refresh path and authenticated bootstrap flow
Primary goals
Make logged-in page refresh fast and production-grade
Reduce perceived refresh load time to around 1–2 seconds
Reduce latency of /api/init and /api/homepage
Remove redundant authenticated bootstrap work
Preserve auth, permissions, and all existing functionality
Produce real measurable improvements, not generic advice
Non-negotiable rules
Do not break auth, permissions, or user-specific filtering
Do not change functionality unless required, and if you do, explain exactly what changed
Do not use mock data
Do not guess — inspect the actual code paths first
Measure before and after
Return real updated code, not pseudocode
Treat this as a production issue, not a superficial frontend tidy-up
Critical diagnosis task

You must specifically investigate why:

login flow is faster
but browser refresh while already authenticated is slow

Assume the root cause may be in one or more of these:

session restoration
auth middleware overhead
repeated user/company/profile lookups
redundant bootstrap requests
overlap between /api/init and /api/homepage
serial dependency between init and homepage data
duplicate frontend fetches on refresh
cache miss or cache bypass on authenticated refresh
repeated permission or role loading
protected route checks that redo DB work unnecessarily
Phase 1 — Map the authenticated refresh boot sequence

Inspect the actual refresh path while logged in.

Document step by step:

what request fires first
what triggers /api/init
what triggers /api/homepage
whether /api/homepage depends on /api/init
whether they fetch overlapping data
whether shared user/company/session context is resolved more than once
whether the frontend blocks on init before rendering usable data
whether refresh causes duplicate requests due to useEffect, route guards, auth hydration, or repeated mounts

Provide a concise map of the full authenticated refresh sequence.

Phase 2 — Add instrumentation before refactoring

Add precise timing logs to:

/api/init
/api/homepage
any shared auth/session middleware
any shared helper functions these routes call

Measure:

total request time
auth/session resolution time
company lookup time
user/profile lookup time
permissions/roles lookup time
DB query time
transform time
cache hit/miss
any external call time
response serialization time if relevant

Use logs like:

const t0 = performance.now();
console.log(`[perf] /api/init start`);

const t1 = performance.now();
// auth/session lookup
console.log(`[perf] /api/init auth lookup: ${performance.now() - t1}ms`);

const t2 = performance.now();
// db query
console.log(`[perf] /api/init db query: ${performance.now() - t2}ms`);

console.log(`[perf] /api/init total: ${performance.now() - t0}ms`);

Or use console.time() / console.timeEnd() if more appropriate.

Do not optimise blindly. Find the actual bottleneck first.

Phase 3 — Remove overlap and duplicate bootstrap work

Refactor the authenticated refresh path so the same protected data work is not being done twice.

Investigate whether /api/init and /api/homepage overlap in any of these:

session/user lookup
company/workspace lookup
role/permission lookup
profile lookup
settings/setup lookup

If they overlap:

centralise the shared bootstrap logic
avoid duplicate DB reads
either fold essential init data into /api/homepage
or make /api/init extremely lightweight and cacheable
remove /api/init entirely from dashboard refresh path if it is unnecessary

Goal: the dashboard refresh path should make one primary authenticated bootstrap request where possible.

Phase 4 — Optimise auth/session middleware

Inspect all middleware used by protected routes.

Check for:

repeated session validation
repeated token parsing
repeated DB calls for user/company/workspace context
repeated permission checks that hit the DB
expensive setup executed on every protected request

Refactor so:

auth resolution is efficient
shared request context is computed once per request
repeated lookups are avoided
middleware remains correct and secure
Phase 5 — Optimise /api/init and /api/homepage

For these routes:

remove unnecessary sequential awaits
replace independent async calls with Promise.all() where safe
remove redundant queries
remove SELECT *
return only the fields the frontend actually uses
eliminate N+1 queries
reduce large payloads
reduce unnecessary transforms or loops
restrict date ranges if jobs or dashboard summaries use large datasets

Example principle:

const [user, company, permissions] = await Promise.all([
  getUser(userId),
  getCompany(companyId),
  getPermissions(userId),
]);

Only parallelise work when there is no required ordering.

Phase 6 — Add safe short-lived caching

Add production-safe caching for expensive authenticated bootstrap data.

Requirements:

cache per user
include company/workspace/date range in cache key if relevant
no user data leakage
TTL 30–60 seconds where safe
log cache hits and misses
cache shared bootstrap context if both init and homepage need it
do not cache highly volatile or security-sensitive data incorrectly

Example pattern:

const key = `homepage:${userId}:${companyId}`;
const cached = cache.get(key);

if (cached && cached.expiresAt > Date.now()) {
  console.log(`[cache] hit ${key}`);
  return res.json(cached.data);
}

console.log(`[cache] miss ${key}`);

Reuse any existing caching library if present rather than building weak ad hoc global state.

Phase 7 — Frontend refresh-path refactor

Inspect the frontend homepage and authenticated app bootstrap.

Check for:

refresh triggering both init and homepage when one should be enough
waiting for init before fetching homepage
duplicate fetches from multiple components
parent and child components fetching overlapping data
effects firing twice
unstable dependencies causing re-fetch loops
auth hydration triggering unnecessary second requests

Refactor so:

the layout renders immediately
the logged-in refresh path uses one main bootstrap request
duplicate requests are eliminated
non-essential data does not block usability
skeletons/placeholders are used where appropriate
the page becomes usable quickly even if secondary data continues loading
Phase 8 — Database/query optimisation

For all queries used in the authenticated refresh path:

select only required columns
add indexes matching real filters and joins
avoid full table scans
avoid repeated round trips
combine queries where appropriate
remove per-row follow-up queries
optimise date-based job lookups
ensure user/company filters are indexed

Only recommend indexes that match the real queries in the code.

Provide exact SQL, for example:

CREATE INDEX IF NOT EXISTS idx_jobs_user_date
ON jobs (user_id, job_date);

CREATE INDEX IF NOT EXISTS idx_profiles_user_company
ON profiles (user_id, company_id);
Phase 9 — Validation and correctness

After refactoring:

verify auth still works
verify permissions still work
verify no user data leakage through cache
verify refresh works correctly when logged in
verify logout/login flow still works
verify edge cases and empty states
verify response shapes still match frontend expectations
Required output

You must provide all of the following:

1. Exact diagnosis

Explain clearly why logged-in refresh is slower than logout/login flow.

2. Bottleneck breakdown

Show where time was actually being spent in:

/api/init
/api/homepage
auth/session middleware
shared helpers
3. Before vs after timings

Include:

total request time
DB time
transform time
cache impact
any middleware overhead
4. Real code changes

Provide the actual updated backend and frontend code.

5. Index recommendations

Provide only real index recommendations that match the actual queries found.

6. Brief rationale

For each major change, explain in 1–3 sentences why it improves performance.

Success criteria

The work is only complete if:

logged-in refresh becomes materially faster
the dashboard becomes usable in roughly 1–2 seconds
/api/init and /api/homepage are significantly improved
redundant bootstrap work is removed
caching is safe and working
auth and correctness are preserved
Final instruction

Work in this order:

inspect → instrument → measure → remove duplication → optimise → measure again

Do not give me a generic checklist.
Do not stop at surface-level frontend changes.
Treat this as a production investigation into an authenticated refresh bottleneck.