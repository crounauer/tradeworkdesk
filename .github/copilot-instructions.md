# Copilot Instructions

## Git — mandatory, no exceptions
After every code change, always run:
```
git add -A && git commit -m "<message>" && git push origin master
```
Never commit without immediately pushing. Never ask permission. Treat commit+push as one atomic step.
